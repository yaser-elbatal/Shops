import { fonts, moderateScale } from "@common";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    title: {
        fontSize: moderateScale(18),
        fontFamily: fonts.bold,
    }
});